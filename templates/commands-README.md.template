# 自定义斜杠命令

这个目录用于存放自定义的斜杠命令，可以通过 `/command-name` 调用。

## 如何创建命令

1. 在此目录创建 Markdown 文件
2. 文件名即为命令名（如 `deploy.md` → `/deploy`）
3. 文件内容是命令的提示词

## 命令示例

### status.md → `/status`
```markdown
检查当前项目状态：
1. Git 分支和未提交的更改
2. 依赖项状态
3. 测试状态
4. 部署状态
```

## 最佳实践

- ✅ 命令名简洁明了
- ✅ 提示词具体且可执行
- ✅ 包含使用示例
- ❌ 避免过于复杂的命令

## 使用方式

在 Claude Code 中直接输入：
```
/status
/deploy
/test
```