# 自定义技能

此目录存放项目专用的简单技能。

## 目录结构

每个技能一个独立目录：

```
skills/
├── deploy/
│   ├── SKILL.md      # 技能执行文件（简洁）
│   └── README.md     # 技能说明文档（详实）
├── test/
│   ├── SKILL.md
│   └── README.md
└── README.md         # 本文件
```

## 文件职责

### SKILL.md - 技能执行文件

**简洁、可执行**，供 AI 直接执行。

**必需内容**:
- Frontmatter（name、description）
- 执行步骤（清晰、可操作）

**示例**:
```markdown
---
name: deploy
description: 部署应用到生产环境
---

# 部署执行

1. 运行测试：`npm test`
2. 构建镜像：`docker build -t app:latest .`
3. 推送镜像：`docker push app:latest`
4. 更新部署：`kubectl apply -f deployment.yaml`
5. 验证健康：`kubectl get pods`
```

### README.md - 技能说明文档

**详实、全面**，供人类开发者参考。

**应包含**:
- 技能用途
- 使用场景
- 参数说明
- 前置条件
- 注意事项
- 示例用法

**示例**:
```markdown
# Deploy 技能

## 用途
自动化部署应用到生产环境。

## 使用场景
- 代码合并到 main 分支后
- 发布新版本时
- 紧急热修复时

## 参数
| 参数 | 说明 | 默认值 |
|------|------|--------|
| environment | 部署环境 | production |
| version | 版本号 | latest |

## 前置条件
- 已配置 kubectl 访问权限
- Docker 镜像仓库访问凭证
- 通过所有测试

## 注意事项
- 部署前必须通过所有测试
- 生产环境部署需要团队审批
- 部署后验证服务健康状态

## 示例
```
/deploy environment=production version=1.2.0
```
```

## 创建新技能步骤

1. 创建技能目录：`mkdir skills/my-skill`
2. 创建 `SKILL.md`（简洁的执行步骤）
3. 创建 `README.md`（详实的说明文档）
4. 重启 Claude Code 会话

## 技能调用

```
/技能名
```

带参数：
```
/技能名 参数1=值1 参数2=值2
```

## 最佳实践

- ✅ SKILL.md 简洁，只写执行步骤
- ✅ README.md 详实，覆盖所有细节
- ✅ 技能目录名与 frontmatter name 一致
- ❌ 不要在 SKILL.md 中堆砌说明文档
- ❌ 不要在 README.md 中重复执行步骤

## 示例技能

参考现有技能目录结构：
- `skills/deploy/SKILL.md` + `README.md`
- `skills/test/SKILL.md` + `README.md`
