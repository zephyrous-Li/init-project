# 测试规范

## 测试原则

### 测试金字塔
- 大量单元测试：快速、隔离、可靠
- 适量集成测试：验证组件协作
- 少量 E2E 测试：验证关键用户流程

### FIRST 原则
- **Fast**：测试应该快速运行
- **Independent**：测试应该独立，不依赖其他测试
- **Repeatable**：测试应该可重复，在任何环境都能通过
- **Self-Validating**：测试应该有明确的通过/失败结果
- **Timely**：测试应该及时编写（TDD：先写测试）

## 单元测试

### 测试覆盖
- 所有公共方法都应该有测试
- 测试正常路径和异常路径
- 测试边界条件

### 测试结构
使用 Given-When-Then 模式：
```typescript
describe('UserService', () => {
  describe('authenticate', () => {
    // Given: 设置初始状态
    const userService = new UserService();

    it('should return user for valid credentials', () => {
      // When: 执行操作
      const result = userService.authenticate('user', 'pass');

      // Then: 验证结果
      expect(result).toBeDefined();
      expect(result.username).toBe('user');
    });
  });
});
```

## 集成测试

### 测试范围
- 测试组件之间的交互
- 测试数据库集成
- 测试 API 端点

### 测试隔离
- 使用测试数据库
- Mock 外部服务
- 清理测试数据

## E2E 测试

### 测试场景
- 只测试关键用户流程
- 登录/注册流程
- 核心业务流程

### 测试稳定性
- 使用明确的等待策略
- 避免硬编码的时间延迟
- 使用测试数据工厂

## 测试覆盖率

### 覆盖率目标
- 语句覆盖率：> 80%
- 分支覆盖率：> 70%
- 函数覆盖率：> 90%

### 覆盖率例外
- 配置文件
- 类型定义
- 已知的第三方集成

## Mock 和 Stub

### 使用场景
- Mock 外部 API 调用
- Stub 数据库查询
- Spy 函数调用

### 最佳实践
- 不要 Mock 被测系统
- 保持 Mock 简单
- 验证 Mock 的交互

## 持续集成

### CI 配置
- 每次提交都运行测试
- 测试失败阻止合并
- 生成覆盖率报告

### 测试策略
- 快速测试在每次提交时运行
- 慢速测试在合并前运行
- 定期运行完整测试套件